import React from "react";
import Hero from "../components/Hero";

const Home = () => (
  <div>
    <Hero />
    <p>
      Welcome to our React app! Explore different pages using the navigation
      bar.
    </p>
  </div>
);

export default Home;
